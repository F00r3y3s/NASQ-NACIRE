import { RouteLoadingState } from "@/components/shell/route-loading-state";

export default function PublicLoading() {
  return (
    <RouteLoadingState
      description="Preparing live discovery, AI, and public intelligence records."
      title="Loading Public Workspace"
    />
  );
}
